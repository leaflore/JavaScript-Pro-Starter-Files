// Summary

// Creating your own inheritance

// Resetting the constructor

// Calling the Super Constructor

// Intermediate Function Inheritance

// Method Overriding

// Polymorphism

// When to Use Inheritance

// Mixins

    // Different types of mixin methods include:
    // 1. Object mixins: directly assign properties and methods from one object to another.
    // 2. Function mixins: use functions to add behavior to objects.
    // 3. Class mixins: use classes to encapsulate reusable behavior and extend other classes.

    // Delegation mixins: delegate behavior to another object rather than copying it.